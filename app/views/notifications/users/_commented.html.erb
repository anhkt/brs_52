<% if notification.seen? %>
  <div>
    <%= notification.user.email %> <%= notification.action %> your review!
  </div>
<% else %>
  <div>
    <strong>
      <%= notification.user.email %> <%= notification.action %> your review!
    </strong>
  </div>
<% end %>
