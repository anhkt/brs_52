<%= form_for [review, @comment], remote: true do |f| %>
  <%= render "shared/errors_messages", object: f.object %>
  <% if f.object.new_record? %>
    <%= f.hidden_field :user_id, value: current_user.id %>
    <%= f.hidden_field :review_id, value: review.id %>
  <% end %>

  <div class="form-group">
    <%= f.text_area :content, placeholder: t("comments.header.content"),
      class: "form-control" %>
  </div>

  <%= f.submit t("buttons.comment"),
    data: {disable_with: t("buttons.commenting")},
    class: "btn btn-primary" %>
<% end %>
