<% provide :title, @user.name %>

<div class="container">
  <div class="col-md-4">
    <h3><%= user_avatar @user %> <%= @user.name %></h3>
    <section class="stats">
      <%= render "shared/stats" %>
    </section>
    <%= render "follow_form" %>
  </div>
</div>
